import { ExcludeUserPipe } from './exclude-user.pipe';

describe('ExcludeUserPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeUserPipe();
    expect(pipe).toBeTruthy();
  });
});
